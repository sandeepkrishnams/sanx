# sanx — Large-scale Python Async Web Server Skeleton

This repository contains a minimal, practical Python async web server skeleton with production
considerations: TLS, graceful shutdown, middleware, routing, and worker process model.

Quick start:

```bash
cd webserver
python -m sanx.core
```

See `docs/` for architecture, PRD, API, and runbook information.
